<%= form_for(about, url: abouts_path) do |form| %>
  <% if about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(about.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
      <% about.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <%= render 'shared/editor_toolbar'%>
  <div class="field">
    <%= form.text_area :text, {id: :about_text , class: "form-control",placeholder: "仅支持Markdown格式填写内容",rows: 10} %>
  </div>



  <div class="actions">
    <%= form.submit "保存" ,{class: "btn btn-default"} %>   
    <%= link_to '取消',  abouts_path , {class: "btn btn-default"} %> 
    
  </div>
<% end %>
